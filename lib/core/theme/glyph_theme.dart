import 'package:flutter/material.dart';

class GlyphTheme {
  static const Color offColor = Color(0xFF000000);
  static const Color lowBrightness = Color(0xFF1C1C1C);
  static const Color fullBrightness = Color(0xFFFFFFFF);
  static const double pixelSize = 13.05;
  static const double matrixSize = 440.0; // Outer diameter
  static const double innerRadius = 360.0 / 2; // For clipping
}